export interface Home {
    concepto:string,
    monto:number,
    fecha:any,
    tipo:string,
    id:number,
    categoria:string,
}

export interface concepto{
    id:number;
    descripcion: string;
}
